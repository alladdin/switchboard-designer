export class Control {
    id: string;
    name: string;
    description: string;

    constructor(params: any) {
        this.id = params.id;
        this.name = params.name;
        this.description = params.decription;
    }
}
